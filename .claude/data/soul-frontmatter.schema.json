{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://0xhoneyjar.dev/loa/schemas/soul-frontmatter.schema.json",
  "title": "L7 SOUL.md Frontmatter",
  "description": "Frontmatter for SOUL.md per SDD §3.2.5 + cycle-098 Sprint 7. Descriptive identity document, distinct from prescriptive CLAUDE.md. Body MUST contain 5 required sections (## What I am, ## What I am not, ## Voice, ## Discipline, ## Influences); optional sections (## Refusals, ## Glossary, ## Provenance) permitted. Section validation lives in the lib (extract+match), not in this JSON Schema.",
  "type": "object",
  "required": [
    "schema_version",
    "identity_for"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "string",
      "enum": ["1.0"]
    },
    "identity_for": {
      "type": "string",
      "enum": ["this-repo", "construct", "agent", "group"],
      "description": "Subject of the identity document. Determines surfacing scope (this-repo always; others reserved for FU-5 construct-overlay)."
    },
    "provenance": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512,
      "description": "Free-text author/source. Control bytes (0x00-0x1f, 0x7f) are rejected by the lib at parse-time as a defense-in-depth measure (mirrors L6 cycle-098 sprint 6 CYP-F2 closure of Python re.$ trailing-newline bypass)."
    },
    "last_updated": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}(?:T[0-9]{2}:[0-9]{2}:[0-9]{2}(?:\\.[0-9]{1,9})?Z)?$",
      "description": "RFC 3339 date or date-time UTC. Operator-controlled; format-only validation here. cycle-098 Sprint 7 cypherpunk MED-1 closure: removed claim of [0001-01-01, now+24h) bounds enforcement that did not exist in the lib — the schema regex accepts any RFC-3339 shape including 0000-01-01 / 9999-12-31. Operators concerned about plausibility checks should add a CI lint rather than expecting the schema to enforce date semantics."
    },
    "tags": {
      "type": "array",
      "maxItems": 32,
      "items": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_-]{1,40}$"
      }
    }
  }
}
