id: constraint-ids-unique
schema_version: 1
skill: test-skill
category: framework
fixture: loa-skill-dir
description: "All constraint IDs are unique"
trials: 1
timeout:
  per_trial: 60
  per_grader: 30
graders:
  - type: code
    script: pattern-match.sh
    args: ["\"id\":", "constraints.json"]
    weight: 1.0
difficulty: basic
tags: ["framework", "constraint-validation"]
