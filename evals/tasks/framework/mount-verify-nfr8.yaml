id: mount-verify-nfr8
schema_version: 1
skill: test-skill
category: framework
fixture: loa-skill-dir
description: "verify_mount() uses boolean-only API key check (NFR-8 compliance)"
trials: 1
timeout:
  per_trial: 60
  per_grader: 30
graders:
  - type: code
    script: pattern-match.sh
    args: ["ANTHROPIC_API_KEY is set", "mount-loa.sh"]
    weight: 1.0
difficulty: basic
tags: ["framework", "mount", "security", "nfr-8"]
