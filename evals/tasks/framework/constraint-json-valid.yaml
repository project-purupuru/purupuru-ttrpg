id: constraint-json-valid
schema_version: 1
skill: test-skill
category: framework
fixture: loa-skill-dir
description: "constraints.json is valid JSON"
trials: 1
timeout:
  per_trial: 60
  per_grader: 30
graders:
  - type: code
    script: quality-gate.sh
    args: ["constraints"]
    weight: 1.0
difficulty: basic
tags: ["framework", "constraint-validation"]
