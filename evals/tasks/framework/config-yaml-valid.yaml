id: config-yaml-valid
schema_version: 1
skill: test-skill
category: framework
fixture: loa-skill-dir
description: ".loa.config.yaml is valid YAML"
trials: 1
timeout:
  per_trial: 60
  per_grader: 30
graders:
  - type: code
    script: quality-gate.sh
    args: ["config"]
    weight: 1.0
difficulty: basic
tags: ["framework", "config-schema"]
