id: archetype-schema
schema_version: 1
skill: test-skill
category: framework
fixture: loa-skill-dir
description: "All archetype templates have required 'name' and 'context' fields"
trials: 1
timeout:
  per_trial: 60
  per_grader: 30
graders:
  - type: code
    script: pattern-match.sh
    args: ["^name:", "rest-api.yaml"]
    weight: 1.0
difficulty: basic
tags: ["framework", "archetypes", "onboarding-ux"]
