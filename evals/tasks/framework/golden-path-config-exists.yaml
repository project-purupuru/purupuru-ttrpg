id: golden-path-config-exists
schema_version: 1
skill: test-skill
category: framework
fixture: loa-skill-dir
description: ".loa.config.yaml exists"
trials: 1
timeout:
  per_trial: 60
  per_grader: 30
graders:
  - type: code
    script: file-exists.sh
    args: [".loa.config.yaml"]
    weight: 1.0
difficulty: basic
tags: ["framework", "golden-path"]
