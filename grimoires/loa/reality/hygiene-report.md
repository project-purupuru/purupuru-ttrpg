# Code Hygiene Report

> Generated by `/ride` 2026-05-07. Flags potential issues for **HUMAN DECISION**. Nothing has been auto-fixed.

## Summary

| Category | Count | Severity |
|----------|-------|----------|
| Files outside standard directories | 0 | — |
| Potential temporary/WIP folders | 0 | — |
| Commented-out code blocks | 0 | — |
| Potential dependency conflicts | 0 | — |
| **Naming drift between docs** | 1 | **MEDIUM** |
| Pre-genesis state items | 4 | INFO |

## Findings

### H-1 [MEDIUM] · Substrate package naming drift between PRD and README

The PRD names the substrate package **`@purupuru/peripheral-events`**, dir **`packages/peripheral-events/`** (CLAUDE.md aligns).
The README, last updated 2026-05-06, names dir **`packages/peripheral-state/`** (README.md L34).

- `CLAUDE.md` line in `## structure` table: `packages/peripheral-events/ (planned · sprint-1)`
- `README.md` L34: `packages/peripheral-state/ # surface-agnostic substrate · ports + adapters`
- `prd.md` §0 tl;dr: `@purupuru/peripheral-events`
- `prd.md` §0 tl;dr: also `@purupuru/omen-templates` is no longer mentioned in post-flatline PRD; README.md L35 still references `packages/omen-templates/` (superseded)

**Recommended action (operator decision)**: refresh README.md to the post-flatline names before sprint-1. The `omen-templates/` package name is a pre-flatline strawman — the new frame is awareness-layer infrastructure, and templates may move into the substrate or be deferred.

### H-2 [INFO] · Pre-genesis state — entire scaffold uncommitted

`git log` returns "current branch 'main' does not have any commits yet". The entire `.beads/`, `.claude/`, `.loa/`, `grimoires/` tree is staged but uncommitted. This is expected for a freshly-mounted repo.

**Recommended action**: operator's call. A sensible first commit groups the Loa scaffold + PRD as `chore: mount Loa v1.130.0 + post-flatline genesis PRD`.

### H-3 [INFO] · `.gitignore` excludes trajectory `.jsonl` files

`grimoires/loa/a2a/trajectory/*.jsonl` is gitignored. This is healthy for ephemeral ride traces, but means `riding-20260507.jsonl` from this run will not be committed. Trajectory audit (Phase 9) reads it locally during this session only.

### H-4 [INFO] · Old PRD reference at non-canonical path

`prd.md` frontmatter `supersedes: purupuru-ttrpg-blink-frontier-prd-2026-05-06.md` references a file that lived at `~/bonfire/grimoires/bonfire/specs/` (per README's "planning artifacts" section). The superseded PRD is **not** in this repo's `legacy/` or `discovery/` dirs — it lives in the bonfire monorepo. This is not drift; it's an external supersede chain.

**Recommended action (optional)**: if the operator wants the supersede chain auditable from inside this repo, copy the strawman to `grimoires/loa/legacy/purupuru-ttrpg-blink-frontier-prd-2026-05-06.md` with a deprecation banner.

### H-5 [INFO] · Beads + flatline log present, fully wired

`.beads/` directory present (Loa-mounted). One flatline artifact present at `grimoires/loa/a2a/flatline/purupuru-ttrpg-genesis-prd-review-2026-05-07.json` (1141 bytes). Mounting trajectory at `grimoires/loa/a2a/trajectory/mounting-20260507.jsonl` confirms `mounting-framework v1.130.0 mount` action 2026-05-07T19:27:41Z.

## Dead Code

None — there is no code at all. The dead-code philosophy ("flag, don't fix") is moot for this ride; will become relevant after sprint-1.

## What This Ride Did NOT Touch

This ride did NOT modify:
- `prd.md` (the canonical post-flatline genesis PRD; treated as authoritative substrate)
- `README.md` (despite H-1 drift; flagged for operator decision)
- `CLAUDE.md`
- Any file under `.claude/` (System Zone — never edited per zone rules)
- Any flatline or mounting artifact under `grimoires/loa/a2a/`

It only WROTE new files under:
- `grimoires/loa/context/` (new)
- `grimoires/loa/reality/` (new)
- `grimoires/loa/legacy/` (INVENTORY only)
- `grimoires/loa/` (drift-report, consistency-report, governance-report, sdd, trajectory-audit)
- `grimoires/loa/a2a/trajectory/riding-20260507.jsonl` (new · gitignored)
