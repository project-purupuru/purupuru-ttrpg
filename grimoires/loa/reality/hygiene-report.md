# Code Hygiene Report · 2026-05-11

> Generated by `/ride compass`. Flags potential issues for **HUMAN DECISION**. Nothing has been auto-fixed.
> Supersedes 2026-05-07 genesis report.

## Summary

| Category | Count | Severity |
|----------|-------|----------|
| Files outside standard directories | 0 | — |
| Stale build artifacts at root (CRITICAL · 4.2 GB) | 2 | HIGH |
| Backup state files | 1 | LOW |
| Commented-out code blocks | 0 | — |
| Potential dependency conflicts | 0 known | — |
| `@ts-ignore` in source | 0 | — |
| TODO markers | 3 | LOW (sprint-3 backlog) |

## HIGH severity

### `.next.OLD-1778465846/` (1.1 GB) and `.next.OLD2-1778467960/` (3.1 GB)

- **Location**: repo root
- **Total disk**: ~4.2 GB
- **Status**: untracked (visible in `git status`)
- **Origin**: appear to be renamed `.next/` build dirs from prior Next.js builds (Unix epoch suffixes ~ 2026-05-09 and 2026-05-10)
- **Risk**: these dirs contain `.next/types/validator.ts` which polluted `grep @ts-ignore` results during this ride. Indexers, AI agents, and search tools will continue to load them as if they were source.
- **Decision needed**: delete (`rm -rf .next.OLD-*`) or move outside the repo. Recommend operator confirmation before delete since they may have been preserved for a reason (deploy fallback?).

### `.run/simstim-state.json.bak` (20 KB)

- **Location**: `.run/`
- **Status**: untracked
- **Origin**: backup created during a simstim session
- **Decision needed**: low priority; safe to delete if simstim cycle is closed.

## TODO markers

3 markers, all clustered in `packages/medium-blink/src/quiz-config.ts:17,18,81`:
- "rotate-n" answer-rotation mode pending random seed
- "tension-pick" Gumi-curated picks pending her per-Q index sets

These are scoped sprint-3 backlog items — not orphan debt.

## What is NOT flagged

- All source code lives in standard directories (`app/`, `components/`, `lib/`, `packages/`, `programs/`, `scripts/`, `tests/`, `evals/`)
- No commented-out function/const blocks discovered
- No `@ts-ignore` / `@ts-expect-error` in non-vendored source
- No obvious dependency duplication in workspace deps (single root `pnpm-lock.yaml`)
- No `.bak`, `.old`, or `tmp_*` files in source tree (the .bak file is in `.run/` state)

## Recommended actions

1. **Operator decision**: delete `.next.OLD-1778465846/` and `.next.OLD2-1778467960/` (4.2 GB recovered, no functional impact since `.next` regenerates on `next build`).
2. Add `.next.OLD*` and `*.json.bak` to `.gitignore` to prevent re-entry.
3. Investigate whether the rename pattern is automated (some npm/pnpm tools rename old `.next` instead of deleting); if so, add a cleanup hook.
4. The 3 TODO markers in `quiz-config.ts` are sprint-3 backlog · no action required this ride.
