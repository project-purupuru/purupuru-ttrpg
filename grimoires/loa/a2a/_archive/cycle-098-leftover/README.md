# cycle-098-leftover

These files arrived via `chore: update Loa framework v0.6.0 → v1.130.0 (initial sync, allow-unrelated-histories)` (commit `ee7cf8b`, which merged `a7c50ff feat(cycle-098-sprint-2)` into purupuru-ttrpg's history).

They describe **Loa framework** cycle-098-agent-network sprint-2 work (L2 cost-budget-enforcer + reconciliation cron + daily snapshot) — completely unrelated to purupuru's sprint-2 (claim_genesis_stone + Metaplex CPI on devnet).

Archived 2026-05-08 at start of purupuru sprint-2 implementation to keep `grimoires/loa/a2a/sprint-2/` clean for the actual sprint reviewer report.

Kept in tree (rather than `git rm`) so the framework lineage stays browsable in this repo without going to the upstream. Safe to delete entirely if the foreign content is ever a problem.
