# Consistency Report · 2026-05-11

> Generated by `/ride compass` against the post-shipped state. Supersedes 2026-05-07 doc-only consistency report.

## Score: **8 / 10**

The codebase is consistent in style, file naming, and entity vocabulary. Minor inconsistencies cluster around (1) the WorldEvent variant count between PRD and code, (2) the framework version pin, (3) tie-break order documentation in voice-corpus header vs in-code resolver.

## Naming Conventions

### Entities (substrate · `packages/peripheral-events/src/`)

| Pattern | Examples | Verdict |
|---------|----------|---------|
| Schemas use PascalCase + Effect.Schema constructors | `Element`, `WorldEvent`, `MintEvent`, `WeatherEvent`, `QuizCompletedEvent`, `ClaimMessage`, `StoneClaimedSchema` | ✅ consistent |
| Branded types: `*Pubkey`, `*Hash`, `*Nonce` with `S.brand()` | `SolanaPubkey`, `QuizStateHash`, `ClaimNonce` | ✅ consistent |
| Discriminated union `_tag` keys are PascalCase | `MintEvent`, `WeatherEvent`, `ElementShiftEvent`, `QuizCompletedEvent` | ✅ consistent |
| Element literals are UPPERCASE 4-5 char strings | `"WOOD" | "FIRE" | "EARTH" | "METAL" | "WATER"` | ✅ consistent |

### Functions

| Pattern | Examples | Verdict |
|---------|----------|---------|
| Verb-first camelCase | `archetypeFromAnswers`, `quizStateHashOf`, `eventIdOf`, `verifyEventId`, `buildClaimMessage`, `signClaimMessage`, `verifyClaimSignature`, `signQuizState`, `verifyQuizState` | ✅ consistent |
| Type-conversion helpers: `xToY` form | `elementToByte`, `byteToElement` | ✅ consistent |
| Predicate functions return boolean: `event*References*` | `eventReferencesPuruhani` | ✅ consistent |

### Files

| Pattern | Examples | Verdict |
|---------|----------|---------|
| Effect/TS source: kebab-case `.ts` | `world-event.ts`, `claim-message.ts`, `event-id.ts`, `bazi-quiz-state.ts` | ✅ consistent |
| API routes: kebab-case dirs ending in `route.ts` (Next 16 App Router) | `quiz/start/route.ts`, `mint/genesis-stone/route.ts` | ✅ consistent |
| Tests: `*.test.ts` for unit, `*.spec.ts` for E2E | `pentagram.test.ts`, `observatory.spec.ts` | ✅ consistent |
| React components: PascalCase `.tsx` | `PuruhaniAvatar.tsx`, `KpiStrip.tsx`, `ActivityRail.tsx`, `IntroAnimation.tsx`, `MobileBottomPanel.tsx` | ✅ consistent |
| Lib helpers: kebab-case `.ts` | `env-check.ts`, `nonce-store.ts`, `sponsored-payer.ts` | ✅ consistent |

### CSS / tokens

| Pattern | Examples | Verdict |
|---------|----------|---------|
| Element token names use `puru-{element}-{shade}` | `bg-puru-fire-vivid`, `font-puru-display` (per CLAUDE.md L34) | ✅ consistent |
| Per-element breathing rhythms `--breath-{element}` | `--breath-fire: 4s` (CLAUDE.md L29) | ✅ consistent |
| OKLCH palette · 5 elements × 4 shades | tint / pastel / dim / vivid | ✅ consistent |

## Conflicts and Improvement Opportunities

### C-1 · WorldEvent variant count discrepancy
- PRD §tl;dr says "3 v0 variants"
- Code (`world-event.ts:71`) has 4: Mint, Weather, ElementShift, QuizCompleted
- **Improvement**: update PRD; or add the 4th variant explanation

### C-2 · OracleSource literals are undocumented
- Code (`world-event.ts:19`): `"TREMOR" | "CORONA" | "BREATH"`
- These names appear nowhere in PRD/SDD/README
- **Improvement**: add a one-line comment or vocabulary entry mapping each oracle to its semantic role

### C-3 · Tie-break order documented in two places
- `voice-corpus.ts:23-24` (header comment)
- `bazi-resolver.ts` (presumably the actual implementation)
- **Improvement**: add a unit test asserting the implementation matches the documented order; lock down via golden-file test

### C-4 · BLINK_DESCRIPTOR canonical home
- `packages/medium-blink/src/` has the local descriptor
- README L243 calls out the upstream PR as deferred
- **Improvement**: file a tracking issue in `freeside-mediums` so the upstream contribution doesn't get lost

### C-5 · Test-count claim vs code reality
- README L317: "127 unit tests"
- Code: ~82 *.test.ts files (could equal more `it()` blocks; not verified this ride)
- **Improvement**: replace hardcoded counts with an auto-generated badge or remove the count

## Breaking-Change Flags (DO NOT IMPLEMENT THIS RIDE)

| # | Pattern | Risk if changed |
|---|---------|------------------|
| BC-1 | `CLAIM_MESSAGE_SIGNED_BYTES = 98` | Changes break Anchor program byte-reconstitution at `lib.rs:131-200`; on-chain rejection of all newly-built claims |
| BC-2 | `_tag` discriminator field on WorldEvent variants | Indexer (zerker's radar) consumes `_tag`; rename = silent indexer break |
| BC-3 | `Element` literals (5 uppercase strings) | Hardcoded in CSS tokens, art file paths (`public/art/stones/{element}.png`), Anchor program element-byte mapping |
| BC-4 | KV nonce key shape | NX EX 300 atomic claim depends on key naming; collision = double-mint |
| BC-5 | `CLAIM_SIGNER_PUBKEY` hardcoded in `lib.rs` | Changing the env keypair without redeploying the program = on-chain rejection |

## Cross-Cutting Strengths

- Effect Schema discriminated unions everywhere → schema validation at every boundary
- HMAC-validated URL state → no server-side session needed
- Three-keypair model is documented in code (lib.rs doc-comment), env-check, and README — full traceability
- Substrate vs presentation separation is reflected in directory structure (`packages/peripheral-events/` vs `app/`)
- Tests organized by category (unit/integration/e2e/security/property/perf/red-team) — disciplined
