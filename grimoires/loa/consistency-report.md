# Consistency Report

> Generated by `/ride` 2026-05-07. Code-pattern analysis is bounded by the absence of application code; this report focuses on **doc-level consistency** across `prd.md`, `README.md`, and `CLAUDE.md`.

## Score

**Overall consistency: 8/10**

- Strong: terminology, doctrine references, tone (ghibli-warm + keeper voice), team-lane assignments
- Weak: substrate package naming drift (1 inconsistency across 3 docs)

## Naming Patterns

### Substrate package — INCONSISTENT (medium)

| Source | Name |
|---|---|
| `prd.md` (canonical) | `@purupuru/peripheral-events` (npm scope) · `packages/peripheral-events/` (dir) |
| `CLAUDE.md` | `packages/peripheral-events/` ✅ aligns with PRD |
| `README.md` | `packages/peripheral-state/` ❌ stale strawman |

**Recommended canonical**: `@purupuru/peripheral-events` / `packages/peripheral-events/`. Refresh `README.md` before sprint-1.

### Solana endpoint app — CONSISTENT

`prd.md` and `CLAUDE.md` agree on `apps/blink-emitter` (next.js, vercel, devnet-locked v0). `README.md` says `apps/web/` (more generic) but doesn't conflict — it could be the same dir under a different alias. **Recommend**: pick one. PRD voice favors `apps/blink-emitter` because the name encodes the medium intent.

### On-chain program — CONSISTENT

All sources: `programs/event-witness/`, anchor, devnet-locked v0, sponsored payer, zero state mutation.

### External repo references — CONSISTENT

All three docs name the same five sibling repos:
- `project-purupuru/game` (codex+gumi pair)
- `project-purupuru/score` (zerker · live)
- `project-purupuru/sonar` (live indexer)
- `project-purupuru/world` aka `world-purupuru` (SvelteKit)
- `0xHoneyJar/freeside-mediums` (medium-registry)

Plus `puruhpuruweather` bot as live upstream broadcast surface.

### Doctrine references — CONSISTENT

Both `CLAUDE.md` and `prd.md` invoke:
- `[[chathead-in-cache-pattern]]`
- `[[chat-medium-presentation-boundary]]`
- `[[environment-surfaces]]`
- `[[puruhani-as-spine]]`
- `[[mibera-as-npc]]` (specifically §6.1)

`README.md` adds `[[metadata-as-integration-contract]]`, `[[wuxing]]`, `[[daily-ritual-loop]]`, `[[weather-puruhani-beat-2]]`, `[[storytelling-game-social-convergence]]`, `[[freeside-modules-as-installables]]`. These are non-conflicting expansions, not contradictions.

## Tone Patterns — CONSISTENT

- All three docs use lowercase prose, ghibli-warm vocabulary (honey, bears, weather, omens), and the operator's bonfire/keeper voice.
- Emoji usage is consistent: 🪺 (substrate), 🌬 (medium / eileen), 🪞 (presentation), 🔥/💧/🌳/⚡/🪨 (wuxing elements), 🪨 (zksoju lane), 🌊 (zerker lane), 🌸 (gumi lane).
- Status banner color is consistent (🟡 = mid-ship, 🟢 = ratified, 🔴 = blocked).

## Team-lane Assignments — CONSISTENT

zksoju / eileen / zerker / gumi roles match across `CLAUDE.md` and `README.md`. `prd.md` adds `gumi voice/lore authority` explicitly in frontmatter.

## Conflicts Identified

| # | Conflict | Sources | Severity | Recommended fix |
|---|---|---|---|---|
| C-1 | `peripheral-events` vs `peripheral-state` | prd.md/CLAUDE.md vs README.md | medium | refresh README to `peripheral-events` |
| C-2 | `apps/blink-emitter` vs `apps/web/` | prd.md/CLAUDE.md vs README.md | low | pick canonical name in sprint-1 plan |
| C-3 | `omen-templates` package mentioned in README | README.md only | low | post-flatline frame supersedes; either retire from README or note as deferred |

## Improvement Opportunities

1. **Pick the canonical name now** for the substrate package and the action endpoint app. Cheap before sprint-1, expensive after.
2. **Retire pre-flatline strawman names** from README.md. The supersede chain already exists in `prd.md` frontmatter.
3. **Add a tiny `legacy/` deprecation note** copying the superseded PRD reference into this repo so the chain is auditable from inside (optional).

## Breaking Changes Flagged

NONE — there is no consumer code. All naming changes can be enacted by editing docs only.
