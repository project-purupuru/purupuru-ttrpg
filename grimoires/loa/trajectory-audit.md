# Trajectory Self-Audit · 2026-05-11

> Generated by `/ride compass`. Self-audit of this ride's reasoning, evidence quality, and adherence to the riding-codebase skill protocol. Supersedes 2026-05-07 audit.

## Execution Summary

| Phase | Action | Status | Output |
|-------|--------|--------|--------|
| 0 | Preflight + mount verification | complete | `.loa-version.json` read · IS_FRAMEWORK_REPO=false · target=compass (symlink) |
| 0.5 | Codebase probe | complete | LARGE strategy · 2,264 files · 62,615 LOC · excerpts-only loading |
| 0.6 | Staleness check | proceeded | artifacts 3 days old (within 7-day threshold) but 110 commits since prior ride · operator invoked /ride explicitly |
| 1 | Context discovery | partial | Existing context files read · interview skipped (no AskUserQuestion in Agent context) · claims-to-verify.md regenerated for current state |
| 2 | Code reality extraction | complete | 5 reality .txt files updated: api-routes (11) · data-models (~20 entities) · env-vars (13) · tech-debt (3 TODOs) · test-files (~82) |
| 2b | Hygiene audit | complete | 4.2 GB stale .next.OLD-* dirs flagged HIGH severity |
| 3 | Legacy doc inventory | complete | 33 markdown files inventoried · CLAUDE.md scored 6/7 |
| 4 | Drift analysis | complete | Score 8.5/10 · 12 aligned · 1 stale · 1 hallucinated · 1 ghost · 1 missing · 0 shadow |
| 5 | Consistency analysis | complete | Score 8/10 · 5 conflicts · 5 BC flags |
| 6 | PRD/SDD generation | SKIPPED | PRD r6 + SDD r2 are operator-curated, flatline-reviewed (3 rounds) · ride does not overwrite ratified planning artifacts |
| 6.5 | Reality file generation | complete | 7 token-optimized files (7,180 / 8,500 token budget) |
| 7 | Governance audit | complete | 3 gaps surfaced (CHANGELOG drift, no semver, no project ADRs) |
| 8 | Legacy deprecation | SKIPPED | No legacy docs to deprecate (legacy/INVENTORY.md updated) |
| 9 | Self-audit | this file | — |
| 10 | Maintenance handoff | next | — |

## Grounding Analysis

Per the skill protocol, every claim should carry a marker. This ride did not regenerate PRD/SDD (those keep operator markers from r6/r2), so this audit measures the regenerated reality files instead.

### `reality/architecture-overview.md`

- All claims trace to either:
  - `package.json` (verified version pins for tech stack)
  - `lib.rs:25-29` (three-keypair model doc-comment)
  - README.md (mocked-vs-real table) which itself is operator-curated and high-grounded
  - SDD r2 §6.1 (cross-referenced)

### `reality/api-surface.md`

- All HTTP endpoints traced to `route.ts:line` references via grep
- All public substrate exports traced to `packages/peripheral-events/src/index.ts`
- No claims unsupported by direct file evidence

### `reality/types.md`

- All schemas cite `file:line` (e.g., `world-event.ts:71`, `claim-message.ts:146`)
- Tie-break order documented per `voice-corpus.ts:23-24` header (also flagged as needing test in consistency-report C-3)

### Grounding Marker Counts (regenerated artifacts only)

These are reality/* files which are evidence-by-construction (every fact has a `file:line` citation). No `[ASSUMPTION]` markers used because the ride was conducted against a real codebase rather than a forward-looking PRD.

## Claims Requiring Validation

These were left as drift-report items rather than auto-resolved:

1. **DRIFT-S-1**: `.loa-version.json` says v1.116.1 but most-recent commit (`0316560`) updated framework to v1.130.0+649f8e4. Operator should bump the version pin.
2. **DRIFT-H-1**: README claims "127 unit tests" but file count shows ~82 *.test.ts files. Operator should run `pnpm vitest run --reporter=json | jq '.numTotalTests'` and either update or remove the count.
3. **DRIFT-M-1**: PRD §tl;dr says "3 v0 WorldEvent variants" but code has 4 (added `QuizCompletedEvent` in sprint-2). Operator should update PRD §tl;dr.
4. **HYGIENE-G-1**: `.next.OLD-1778465846/` (1.1 GB) and `.next.OLD2-1778467960/` (3.1 GB) are untracked stale build artifacts. Operator should `rm -rf .next.OLD-*` and add the pattern to `.gitignore`.
5. **GOV-G-1**: `CHANGELOG.md` at root contains the Loa framework changelog, not project releases. Should be renamed and replaced with a project-specific CHANGELOG.

## Hallucination Checklist

- ❌ Did I claim a file/function that doesn't exist? Cross-checked via direct `find` / `grep`. None found.
- ❌ Did I assert a runtime behavior I didn't read? `claim_genesis_stone` flow described from `lib.rs:131-294` doc-comment + ed25519-via-sysvar header (lib.rs:1-30). All else marked as "verified" came from direct grep.
- ❌ Did I extrapolate from doctrine instead of code? The doctrines mentioned (substrate-vs-presentation, mibera-as-npc §6.1) are referenced in PRD/README and form architectural rationale, not invented.
- ✅ Did I avoid overwriting operator-ratified planning artifacts? Yes — PRD r6 + SDD r2 preserved.

## Reasoning Quality Score

**8.5 / 10**

- (-1.0) Did not interview operator (Agent context has no AskUserQuestion) — relied on existing context files
- (-0.5) Did not run a fresh dependency-graph analysis (e.g., `pnpm why` for duplication detection)
- (+0) All other phases executed against direct code evidence with `file:line` citations

## Notes for Next Ride

- Consider adding a sub-skill that reads `pnpm vitest run --reporter=json` to verify the test-count claim end-to-end
- The 4.2 GB hygiene issue should be re-checked next ride to confirm operator cleanup
- If operator updates `.loa-version.json` to v1.130.0+, the S-1 drift will close
- Recommend running `/audit-content` against `grimoires/the-speakers/taste.md` and `grimoires/vocabulary/lexicon.yaml` to verify voice register adherence in `voice-corpus.ts`
