# Drift Report (Three-Way Analysis) · 2026-05-11

> Generated by `/ride compass`. Compares: Code Reality ⇄ Documentation (README + CLAUDE.md + docs/) ⇄ Operator-Stated Intent (PRD + sprint plans + NOTES.md).
> Supersedes 2026-05-07 genesis report (which was 100% GHOST because no code existed yet).

## Summary

| Drift Type | Count | Severity |
|------------|-------|----------|
| Aligned | 12 | Healthy |
| Missing (code, no docs) | 1 | Low |
| Stale (docs, code changed) | 1 | Medium |
| Hallucinated (docs claim, code lacks) | 1 | Low |
| Ghost (documented, never built) | 1 | Low |
| Shadow (code, totally undocumented) | 0 | — |

**Drift Score: 8.5 / 10** — high alignment between intent, docs, and code. The repo is in a near-shipped state; documentation has tracked the build closely.

## Aligned (sample)

| # | Doc Claim | Code Evidence |
|---|---|---|
| AL-1 | `@purupuru/peripheral-events` substrate package with WorldEvent / ClaimMessage / StoneClaimed schemas | `packages/peripheral-events/src/{world-event,claim-message,stone-claimed,event-id,bazi-quiz-state,bazi-resolver}.ts` |
| AL-2 | Anchor program `claim_genesis_stone` with ed25519 sysvar verify + Metaplex CPI mint | `programs/purupuru-anchor/programs/purupuru-anchor/src/lib.rs:131-294` |
| AL-3 | `app/api/actions/{today,quiz/{start,step,result},mint/genesis-stone}/route.ts` Solana Actions endpoints | All routes verified via grep `^export async function (GET|POST)` |
| AL-4 | Three-keypair model (sponsored-payer / claim-signer / user wallet) | `lib/blink/sponsored-payer.ts` + `lib.rs:25-29` doc-comment |
| AL-5 | KV nonce store with NX EX 300 atomic claim | `lib/blink/nonce-store.ts` + env-check requires KV_REST_API_URL/TOKEN |
| AL-6 | HMAC-SHA256 quiz state · `signQuizState` / `verifyQuizState` | `packages/peripheral-events/src/bazi-quiz-state.ts` |
| AL-7 | 8 questions × 3 answers · 5 archetype reveals · WOOD>FIRE>EARTH>METAL>WATER tie-break | `packages/medium-blink/src/voice-corpus.ts:1-26` (header doc-comment) |
| AL-8 | `app/demo/` X-faithful 3-column recording surface | dir present |
| AL-9 | `app/opengraph-image.tsx` for static OG · plus `app/api/og/route.tsx` placeholder for dynamic OG | both present |
| AL-10 | `app/sitemap.ts` + `public/llms.txt` SEO + AI-readability surface | both present |
| AL-11 | Vercel KV is the only persistence layer | no `pg`/`postgres`/`DATABASE_URL` in source |
| AL-12 | `app/preview/page.tsx` local Blink preview | dir present |

## Stale Docs

### S-1 · `.loa-version.json` not bumped (Severity: Medium)

- **Doc says** (`.loa-version.json`): `"framework_version": "1.116.1"` · `"submodule.ref": "v1.116.1"` · `"last_sync": "2026-05-04T19:45:00Z"`
- **Code/repo reality**: most-recent commit `0316560` reads `chore: update Loa framework (789 commits · 1.130.0 → 1.130.0+649f8e4)`
- **Impact**: any tool that reads `.loa-version.json` to infer the framework version will be wrong by ~14 minor versions.
- **Recommendation**: bump `.loa-version.json` to match the actual submodule HEAD; add a CI check that fails when commit message mentions framework update without bumping the version pin.

## Hallucinated

### H-1 · README "127 unit tests" claim (Severity: Low)

- **Doc says** (`README.md` L317): `pnpm vitest run                              # all unit tests (127 tests)`
- **Code reality**: ~82 *.test.ts/*.spec.ts files in non-vendored locations. The test count (per-file) is at most ~82, though one file may contain multiple `it()` blocks; not verified this ride.
- **Recommendation**: regenerate the count by running `pnpm vitest run --reporter=json | jq '.numTotalTests'` and update the README. Mark cross-doc test counts as auto-generated to prevent staleness.

## Ghost Features

### G-1 · `BLINK_DESCRIPTOR` upstream PR to freeside-mediums (Severity: Low)

- **Doc says** (README "What's mocked vs real" table L243): `BLINK_DESCRIPTOR upstream PR · 🔴 — Local descriptor · upstream contribution to freeside-mediums deferred post-hackathon`
- **Code reality**: there's a local `packages/medium-blink/` providing the descriptor; no upstream PR in `freeside-mediums` referenced.
- **Verdict**: explicitly deferred — not a true ghost; surface as v1 backlog item.

## Missing (Code Without Docs)

### M-1 · 4th `WorldEvent` variant `QuizCompletedEvent` (Severity: Low)

- **Code says** (`world-event.ts:71`): `WorldEvent = S.Union(MintEvent, WeatherEvent, ElementShiftEvent, QuizCompletedEvent)` — 4 variants
- **Doc says** (PRD tl;dr): "WorldEvent discriminated union with 3 v0 variants (mint, weather shift, element surge)"
- **Verdict**: 4th variant `QuizCompletedEvent` was added during sprint-2 but the PRD §tl;dr was not updated.
- **Recommendation**: update PRD §tl;dr to "4 v0 variants (mint, weather, element-shift, quiz-completed)".

## Shadow Code

None detected this ride. The codebase has been documented at-or-near every level (READMEs in evals/, fixtures/, programs/purupuru-anchor/, grimoires/, etc.).

## What's Healthy

This repo is exceptional for a hackathon-stage project: high doc-code alignment, explicit mocked-vs-real table in README, separation-as-moat doctrine surfaced in both architecture diagrams and code organization. The `lib/blink/env-check.ts` preflight pattern is reusable across the team.

## Top Drift Items

1. **S-1** `.loa-version.json` lag (~14 versions)
2. **H-1** README test count (127 vs ~82 files)
3. **M-1** PRD missing the 4th WorldEvent variant
