---
session: 13
date: 2026-05-16
type: kickoff
status: planned
run_id: 20260516-e77b8c
prior_session: 12 (juice — autonomous bears + activationLevel grove)
---

# Session 13 — Substrate · Graduation Tier as User-Truth Backpressure (kickoff)

## Scope

- Build the GRADUATION TIER schema substrate (gold/silver/bronze) mirroring loa-hounfour envelope discipline (tristate verdicts, closed-shape additionalProperties:false, SHA256 quote hashes, x-cross-field-validated)
- Wire the tier system to USER TRUTH CANVAS references via hivemind-laboratory pattern — `linked_utcs: [labs/<domain>/UTC-<slug>.md]`
- Map graduation tiers onto purupuru-codex's existing 4-tier `canon_tier` enum (bronze→speculative, silver→exploratory, gold→established)
- DIG distillation into the-arcade + vfx-playbook + the-easel constructs via parallel subagents
- NO UI work this session — UI sandbox (VFX lab) + heavy-cards-emphasis ship in follow-on session

## Artifacts

- Build doc: `grimoires/loa/specs/enhance-substrate-graduation-utc.md` (combined ARCH + ENHANCE — schema sketch, blast radius, build sequence, design rules, sub-agent dispatch plan)
- Run trail: `.run/compose/20260516-e77b8c/orchestrator.jsonl` (rooms-substrate kickoff trail)

## Prior session (12)

Shipped:
- Autonomous bear colony with steering + FSM (perceive→decide→act)
- activationLevel grove memory (cards grow the forest)
- Operator validated juice on the FEEL surface

This session (13) does NOT extend juice — pivots to substrate.

## Decisions made (Phase 2 PREPLAN crystallization)

| Decision | Choice | Rationale |
|---|---|---|
| First-move scope | substrate + research paired (single session) | operator: "substrate and research are probably the main priority for us · UI feature I can focus on after" |
| Tier definition | hybrid: path-aware default + frontmatter override + UTC linkage | construct-gauntlet's 3-bucket filesystem + hounfour's tristate verdict + observer's UTC link = grounded backpressure |
| Tier semantic | USER-TRUTH BACKPRESSURE (not quality rank) | operator's load-bearing insight: counteract agent forward-generation drift |
| Codex scope | lore + characters + items + entities only · taste tokens stay in compass for now | operator: "taste tokens and UI design are... something I would likely own in a different construct" |
| UTC referencing | by URL (cross-repo contract), no local clone | matches hivemind-laboratory labs/README pattern |
| Construct compounding | dispatch arcade + vfx-playbook + easel subagents in parallel within session | operator: "use subagents so there's more clarity" |
| UI feature shape (next-next session) | Gumi-designed legendary frames + combo highlighting + ENVIRONMENTAL EFFECTS on the map (trees cut, anvil hammered) | operator vision: map IS the visual feedback for game progress |
| VFX sandbox stack (when shipped) | Leva (r3f-native) + react-flow (Phase 2 graph editor) + 6 starter effects | vfx-playbook construct synthesis |

## Execution shape (revised per operator clarification)

NOT fire-and-forget. INTERACTIVE distillation loop · operator-driven thread
pulling via `/dig` · construct upgrades happen FIRST.

**Phase 0 · construct-upgrade pass** (BEFORE distillation):
- 5 constructs (arcade, vfx-playbook, easel, observer, purupuru-codex) get
  upgraded to latest pipe-doctrine v4 patterns from `~/Documents/GitHub/loa-constructs`
  (v2.41.0) + `~/Documents/GitHub/construct-base` (template)
- Operator-gated at each construct boundary (~15-25min × 5 = ~2h)

**Phase 1 · interactive distillation** (per-construct, one topic at a time):
- construct-the-arcade — heavy-card + combo + engagement-loop best practices
- construct-vfx-playbook — composition-graph patterns
- construct-the-easel — existing taste-token tier audit
- Each subagent dispatch is SCOPED (one topic, not a shopping list)
- Subagent returns initial pass + 3-5 pull-threads
- Operator picks threads to chase via `/dig` (operator-driven, not auto)
- Operator brings external artifacts in (screenshots, refs, doc URLs)
- Doctrine entry drafted with subagent + reviewed/greenlit by operator
- Lands in construct doctrine, then next topic / next construct

**Phase 2-4**: schema substrate build · purupuru-codex wiring · operator
activation (full sequence in build doc §"What to Build")

## Backpressure on the operator's brief — what to verify

Operator surfaced 4 OPEN CREATIVE QUESTIONS in the build doc (see §"Open Creative Questions"). Address at session start before code:
1. Schema location: project-local vs spun-out construct repo
2. Auto-demotion on UTC drift: automatic or warn-only
3. Tier badge surfacing: in /battle-v2 or dev-only
4. The-easel split: taste-design as its own construct or doctrine surface

## Reference repos to read in session

- ~/Documents/GitHub/hivemind-laboratory (UTC schema + labs pattern — newly cloned this session)
- ~/Documents/GitHub/loa-hounfour (envelope substrate — already accessible)
- ~/Documents/GitHub/construct-gauntlet (graduation prior art — old, lessons only per operator)
- .claude/constructs/packs/purupuru-codex (already installed in cycle-1)
- .claude/constructs/packs/observer (UTC producer construct)
