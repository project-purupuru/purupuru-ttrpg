# Claims to Verify

> Generated by `/ride` 2026-05-07. Source: existing `grimoires/loa/prd.md` (post-flatline genesis), `CLAUDE.md`, `README.md`. No interactive interview was conducted — the PRD already represents 1 round of flatline-adversarial review and is the authoritative substrate.

## Architecture Claims

| # | Claim | Source | Verification Strategy | Status |
|---|---|---|---|---|
| A1 | Repo is "scaffold-only as of 2026-05-06" | README.md L4 | List all non-vendored files; confirm absence of `apps/`, `packages/`, `programs/` | VERIFIED — only `.gitignore`, `.loa-version.json`, `.loa.config.yaml`, `CLAUDE.md`, `README.md` exist |
| A2 | Planned structure is `apps/web/`, `packages/peripheral-state/`, `packages/omen-templates/`, `grimoires/loa/` | README.md L31-39 | Glob for these dirs | PARTIALLY VERIFIED — only `grimoires/loa/` exists; `apps/`, `packages/` absent |
| A3 | PRD has been flatline-reviewed by 3 models (claude-opus-4-8 + gpt-5.4-codex + gemini-3.0-pro) | prd.md frontmatter, README absent | Inspect `grimoires/loa/a2a/flatline/purupuru-ttrpg-genesis-prd-review-2026-05-07.json` | VERIFIED — flatline log present, 3-model mode confirmed, 0¢ subscription cost |
| A4 | Loa framework v1.130.0 is mounted | CLAUDE.md, .loa-version.json | Read `.loa-version.json` | VERIFIED |
| A5 | The substrate target package will be `@purupuru/peripheral-events` (PRD) — but README plans `packages/peripheral-state/` | prd.md tl;dr vs README L33 | Note rename between docs | DRIFT — naming inconsistency between PRD and README |
| A6 | Ship deadline: Colosseum Frontier 2026-05-11 (4 days from 2026-05-07) | prd.md frontmatter `deadline: 2026-05-11`, CLAUDE.md status | Compute date math | VERIFIED |
| A7 | `freeside-mediums` `medium-registry@0.2.0` already shipped (cycle-R cmp-boundary-architecture, 2026-05-04) | prd.md §2 | Cross-ref operator memory entry `Cycle-R cmp-boundary-architecture FULLY SHIPPED 2026-05-04/05` | VERIFIED via operator memory |
| A8 | The repo will host the Solana anchor witness program at `programs/event-witness/` (devnet only) | CLAUDE.md structure table, prd.md §3.3 | Glob for `programs/` | UNVERIFIABLE — not yet created |

## Domain Claims

| # | Claim | Source | Verification Strategy | Status |
|---|---|---|---|---|
| D1 | Five doctrines are load-bearing: `[[chathead-in-cache-pattern]]`, `[[chat-medium-presentation-boundary]]`, `[[environment-surfaces]]`, `[[puruhani-as-spine]]`, `[[mibera-as-npc]] §6.1` | CLAUDE.md "core doctrines" | These are vault doctrines outside the repo; cite-only here | NON-VERIFIABLE FROM CODE — doctrines live in `~/vault/wiki/concepts/` |
| D2 | `[[mibera-as-npc]] §6.1` enforces "no payment via LLM verdicts · no session keys" | CLAUDE.md, prd.md repeated invocations | Check vault doctrine file | NON-VERIFIABLE FROM CODE |
| D3 | The substrate emits a `WorldEvent` discriminated union with 3 v0 variants (mint, weather shift, element surge) | prd.md tl;dr | No code yet — claim is forward-looking | UNVERIFIABLE — no code |
| D4 | `eventId` is computed as `sha256(canonical_encoded + version + source)` | prd.md tl;dr, FR-1, FR-3 | No code yet | UNVERIFIABLE — no code |
| D5 | Solana witness program: zero state mutation, sponsored-payer, gasless UX, devnet-locked v0 | prd.md §3.3 | No code yet | UNVERIFIABLE — no code |

## Tribal Knowledge

| # | Claim | Handling |
|---|---|---|
| T1 | "MMO death by silence" — operator framing for the *why now* | Surface in PRD, do not code-verify |
| T2 | Voice authority: keeper + vocabulary-bank + herald (per operator decree) | Surface in SDD §contracts |
| T3 | Eileen ratifies architecture; gumi owns voice/lore/wardrobe; zerker owns score read-side | Surface as roles in governance report |
| T4 | The Twitter weather bot `@puruhpuruweather` is the existing surface this repo extends | Note in interfaces.md as upstream |
| T5 | `project-purupuru/game` is the parallel codex+gumi pair repo where game logic lives — this repo CONSUMES game-emitted events, doesn't run game logic | Surface as L1 source in architecture-overview.md |

## Work-in-Progress Status

| # | Claim | Status |
|---|---|---|
| W1 | Sprint 1 not started; 4-day clock to Colosseum Frontier 2026-05-11 | VERIFIED via CLAUDE.md status banner |
| W2 | No git commits yet; entire `.claude/`, `.beads/`, `grimoires/` tree is staged but uncommitted | VERIFIED — `git log` returns "does not have any commits yet"; `git status` shows ~1000 staged files |
| W3 | MVD floor is OPEN per flatline blocker SKP-001 — single-owner risk, 4d clock | VERIFIED in prd.md tl;dr |
| W4 | `BLINK_DESCRIPTOR` PR to `freeside-mediums/protocol` is planned (5th MediumCapability variant alongside Discord/CLI/TG) | UNVERIFIABLE — PR does not yet exist |

## Naming Drift to Surface

- PRD says **`@purupuru/peripheral-events`** (substrate package name)
- README says **`packages/peripheral-state/`** (substrate dir name)
- CLAUDE.md says **`packages/peripheral-events/`** (substrate dir name)

CLAUDE.md aligns with PRD; README is stale by one naming pivot. Surface in drift-report.md.

## Notes

This `/ride` runs against a genesis-stage repo with no application code. Drift analysis collapses to: "everything PRD-described is GHOST relative to the current code state." This is *expected and healthy* for a pre-sprint-1 genesis. The SDD generated below documents the **intended architecture** with `[INFERRED]` markers, not `[GROUNDED]`, because there is nothing to ground against in code.
