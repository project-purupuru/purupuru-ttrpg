# NEGATIVE fixture — NFR-Sec1 violation at schema layer (defaults.review must be advisor)
advisor_strategy:
  schema_version: 1
  enabled: true
  tier_resolution: static
  defaults:
    planning: advisor
    review: executor       # NFR-Sec1 hard-pin violation; schema rejects
    audit: advisor
    implementation: executor
  tier_aliases:
    advisor:
      anthropic: claude-opus-4-7
    executor:
      anthropic: claude-sonnet-4-6
  per_skill_overrides: {}
